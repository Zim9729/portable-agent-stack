# Skill evaluation fixtures

These JSON files define representative prompts and human-readable success criteria. They are intentionally model-agnostic.

Use them when changing skill descriptions, side-effect boundaries, mode selection, or output contracts. A future automated eval harness can consume the same fixtures.
